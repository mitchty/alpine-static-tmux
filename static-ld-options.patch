--- a/pandoc.cabal
+++ b/pandoc.cabal
@@ -424,6 +424,7 @@
   Buildable:       True
 
 Executable pandoc
+  ld-options: -static
   Build-Depends: pandoc,
                  pandoc-types >= 1.17 && < 1.18,
                  base >= 4.2 && <5,
