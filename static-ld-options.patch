--- pandoc.cabal.original
+++ pandoc.cabal
@@ -407,6 +407,7 @@
   Buildable:       True
 
 Executable pandoc
+  ld-options: -static 
   Build-Depends: pandoc,
                  pandoc-types >= 1.16 && < 1.17,
                  base >= 4.2 && <5,
